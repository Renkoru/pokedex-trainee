*09-Test* 

1. Use redux
   - import { createStore } from 'redux';
   - Fix all imports with connect
2. Fix ReduxDevTools 
   - const composeEnhancers = window.__REDUX_DEVTOOLS_EXTENSION_COMPOSE__ || compose;
   - const store = createStore(reducer, /* preloadedState, */ composeEnhancers());
